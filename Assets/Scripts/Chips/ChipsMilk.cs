using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChipsMilk : Chips
{
    // Start is called before the first frame update
    void Start()
    {
        _ingredient = "Milk";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
