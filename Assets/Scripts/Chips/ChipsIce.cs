using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChipsIce : Chips
{
    // Start is called before the first frame update
    void Start()
    {
        _ingredient = "Ice";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
