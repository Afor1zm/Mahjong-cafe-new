using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChipsMarshmallow : Chips
{
    // Start is called before the first frame update
    void Start()
    {
        _ingredient = "Marshmallow";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
